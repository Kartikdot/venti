{
  "name": "venti",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "node dist/server.js",
    "start:dev": "ts-node src/server.ts",
    "copyfiles": "npm run copy:dist && npm run copy:node_modules",
    "copy:dist": "cpr dist ./app/dist -o",
    "copy:node_modules": "cpr node_modules ./app/node_modules -o",
    "clean": "rimraf node_modules && rimraf dist && rimraf app",
    "package": "npm run clean && npm i && npm run build && npm run copyfiles"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "express": "^4.17.2",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.7.1",
    "pg-hstore": "^2.3.4",
    "typeorm": "^0.2.41",
    "typescript": "^4.5.4"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/express": "^4.17.13",
    "@types/jsonwebtoken": "^8.5.7",
    "@types/node": "^17.0.8",
    "cpr": "^3.0.1",
    "dotenv": "^14.2.0",
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0"
  }
}
